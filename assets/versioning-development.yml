# ============================================
# Autogenerated. Modify it to your needs.
# ============================================

name: Versioning Development

on:
  push:
    branches:
      - develop

jobs:
  versioning:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0